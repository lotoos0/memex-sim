Legacy components from the pre-rewrite architecture.

These files are intentionally not used by the current router/pages.
Keep them only as reference while migrating features to the new token/pulse layout.
